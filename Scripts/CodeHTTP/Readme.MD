Este script sirve para leer un archivo CSV, tomar la columna URL, y verificar cuáles enlaces responden con un código HTTP esperado (por defecto 200 OK). 
Las URLs que respondan correctamente se guardan en uno o varios archivos de salida (por partes), para que te quedes únicamente con las filas “válidas” (URLs que funcionan).
